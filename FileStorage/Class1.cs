namespace FileStorage;

public class FileStorageManager
{
    private string _defaultPath;

    public FileStorageManager(string defaultPath)
    {
        _defaultPath = defaultPath;
    }

    public void SaveFile(string file)
    {
        
    }
}